J'ai retaillé vite fait l'architecture :

maintenant celle des reducers est consistante avec celle des actions,
l'équivalent de l'init du reducers se trouve mnt ds index.js
(les socket et peer ont leur place ds index.js, pas ailleurs)

l'architecture des middlewares est consistante avec celle des reducers
( on fait l'init ds le index.js )

début d'implémentation des classes Game et Player
ds webSocket, la var allGames sert pour les tests, il faudra la gérer autrement
(qd on recoit une requete en GET sur /game ou autre on crée un new Game et on push ds allGames )

A faire Pour moi :

le container Home.js est agrémenté d'un RTCConn pour l'exemple, le faire plus propre

je vais implémenter un système de gestions des actions cote front
de facon a ce que ce soit consistant avec le back
(cf le /game avec path: /join etc etc)

