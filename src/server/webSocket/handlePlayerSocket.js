/**
 * handle player socket input
 *
 * @param {Object} data - the data
 * @return {void}
 */
export default async function (data) {
  if (data.path === '/new') {
    // do new player

    // io.sockets.to().emit('', );
  }
}
